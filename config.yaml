# Configuration
datasets:
  structure: coco
  default_format: json
  output_dir: "data"

dvc:
  repo_path: "."

cvat:
  url: "https://cvat.example.com"
  username: "${CVAT_USERNAME}"
  password: "${CVAT_PASSWORD}"
